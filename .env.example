# =============================================================================
# SigNoz Starter Pack — переменные окружения
# Скопируйте в .env и подставьте свои значения.
# =============================================================================

# --- SigNoz endpoint (обязательно) ---
# Cloud:   ingest.<REGION>.signoz.cloud (порт 443)
# Self-hosted: ваш хост и порт (например localhost:4317 для gRPC)
SIGNOZ_OTEL_ENDPOINT=ingest.region.signoz.cloud:443
# Для self-hosted без TLS:
# SIGNOZ_OTEL_ENDPOINT=localhost:4317

# Использовать TLS (true для Cloud, false для локального self-hosted)
SIGNOZ_OTEL_TLS=true

# Ingestion Key (только для SigNoz Cloud; для self-hosted оставьте пустым)
SIGNOZ_INGESTION_KEY=

# --- Опционально: коллектор (для docker-compose/скриптов) ---
# Путь к конфигу коллектора (полный, docker-only или vm)
OTEL_COLLECTOR_CONFIG=collector/config.full.yaml

# --- Alerting (Self-Hosted SigNoz: Alertmanager) ---
# Внешний URL SigNoz для ссылок в алертах (например https://signoz.example.com)
# SIGNOZ_ALERTMANAGER_SIGNOZ_EXTERNAL__URL=https://signoz.example.com

# SMTP для email-уведомлений
# SIGNOZ_ALERTMANAGER_SIGNOZ_GLOBAL_SMTP__SMARTHOST=smtp.example.com:587
# SIGNOZ_ALERTMANAGER_SIGNOZ_GLOBAL_SMTP__FROM=alerts@example.com
# SIGNOZ_ALERTMANAGER_SIGNOZ_GLOBAL_SMTP__AUTH__USERNAME=
# SIGNOZ_ALERTMANAGER_SIGNOZ_GLOBAL_SMTP__AUTH__PASSWORD=

# --- Логи хоста (пути к логам для filelog; через запятую для нескольких) ---
# Linux примеры:
# HOST_LOG_PATHS=/var/log/syslog,/var/log/myapp/*.log
# Windows (в конфиге коллектора указываются пути на хосте)
# HOST_LOG_PATHS=C:\Logs\app\*.log
