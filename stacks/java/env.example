# Java / JVM — переменные для экспорта телеметрии в SigNoz (OTLP)
# Задайте JVM options (-Dotel.*) или экспортируйте в окружение.
#
# Документация: https://signoz.io/docs/instrumentation/java/

OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
OTEL_EXPORTER_OTLP_PROTOCOL=grpc

OTEL_SERVICE_NAME=my-java-service

OTEL_TRACES_EXPORTER=otlp
OTEL_METRICS_EXPORTER=otlp
OTEL_LOGS_EXPORTER=otlp

# Трассировка: семплирование и атрибуты (см. docs/TRACING_SETUP.md)
OTEL_TRACES_SAMPLER=parentbased_traceidratio
OTEL_TRACES_SAMPLER_ARG=0.1
OTEL_RESOURCE_ATTRIBUTES=deployment.environment=production,service.version=1.0.0
OTEL_PROPAGATORS=tracecontext,baggage

# Логи: уровень — SLF4J/Logback; для фильтра по кодам ошибок — MDC или поле в JSON
# (например error.code), см. docs/LOGGING_FILTERING.md

# Пример JVM аргументов:
# -Dotel.exporter.otlp.endpoint=http://localhost:4317
# -Dotel.exporter.otlp.protocol=grpc
# -Dotel.service.name=my-java-service
# -javaagent:path/to/opentelemetry-javaagent.jar
